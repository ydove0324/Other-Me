from __future__ import annotations

import re
import logging

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.ai_config import PromptTemplate

logger = logging.getLogger(__name__)

_TEMPLATE_CACHE: dict[str, str] = {}

FALLBACK_TEMPLATES: dict[str, str] = {
    "persona_generation": """你是一位资深的心理画像分析师。根据以下用户的自我标签和问卷回答，生成一份结构化的性格画像。

## 用户选择的标签
{{tags}}

## 用户的问卷回答
{{answers}}

请以 JSON 格式输出，包含以下字段：
{
  "persona_summary": "一段200字左右的人物画像描述，用第二人称'你'来写，像朋友一样温暖地描述这个人",
  "personality_traits": ["特质1", "特质2", ...],
  "core_values": ["价值观1", "价值观2", ...],
  "life_stage": "当前人生阶段的简短描述",
  "emotional_patterns": ["情绪模式1", "情绪模式2", ...],
  "key_relationships": "对人际关系的偏好和模式",
  "dreams_and_fears": "内心深处的渴望与恐惧"
}""",
    "life_timeline": """你是一位才华横溢的叙事作家。根据以下信息，为这个人构建一条"未选择之路"的人生时间线。

## 人物画像
{{persona}}

## 人生分岔点
- 时间：{{fork_date}}
- 标题：{{fork_title}}
- 背景：{{fork_description}}
- 实际的选择：{{actual_choice}}
- 未走的那条路：{{alternative_choice}}
- 当时的情绪：{{emotional_context}}

## 要求
从分岔点开始，想象**如果选择了"未走的那条路"**，这个人的人生会怎样展开。生成10-15个关键人生事件节点，时间跨度从分岔点延续到当前（{{current_year}}年）。

**重点**：每个事件必须是"未走的路"上的事（如选了考公后的备考、入职、工作起伏等），不要编排大量"当时如何纠结、如何做出真实选择"的回溯；用户想看的是另一种可能的展开，而不是真实心路历程的重述。

每个事件要真实、细腻、有情感温度。既有高光时刻也有低谷和挣扎——因为每条路都有代价。

**硬性约束**：事件之间必须逻辑自洽，不得前后矛盾（如居住状态、感情状态、学业职业等）；不要编造用户未提及的具体情节（如期中考试、见家长等），只基于问卷和分岔点信息推演；事件中的人物用代称（室友、那个她、好朋友、同事等），不要编造人名；用户喜欢的菜在全时间线中最多出现一次。

请以 JSON 格式输出：
{
  "overview": "这条未选择之路的一句话概述",
  "events": [
    {
      "event_date": "YYYY-MM-DD",
      "title": "事件标题",
      "summary": "50-100字的事件摘要",
      "emotional_tone": {"primary": "主要情绪", "secondary": "次要情绪", "intensity": 0.8}
    }
  ]
}""",
    "event_detail": """你是一位细腻的第一人称叙事作家。请根据以下信息，为这个时间线事件写一段详细的叙事。

## 人物画像
{{persona}}

## 人生分岔点背景
{{fork_context}}

## 此前的人生经历
{{previous_events}}

## 当前事件
- 时间：{{event_date}}
- 标题：{{event_title}}
- 摘要：{{event_summary}}
- 情绪基调：{{emotional_tone}}

## 写作要求
1. 用第一人称"我"来写，仿佛这就是"另一个我"在写日记或回忆录
2. 字数500-800字
3. 要体现人物画像中的性格特征和价值观
4. 自然引用之前的经历，保持叙事连贯
5. 情感真实，不回避矛盾和代价
6. **不要**在叙事里反复回溯"现实中的选择"或纠结过程；偶尔可有一两句自然带过即可，叙事主体应是这条未走的路上的具体经历

**硬性约束**：与前后事件逻辑一致，不矛盾；不编造用户未提及的具体情节；人物用代称（室友、那个她、好朋友等），不编造人名；喜欢的菜不反复提及。

直接输出叙事文本，不需要 JSON 格式。""",
    "life_blocks": """你是一位才华横溢的叙事作家，擅长用细腻的笔触书写朴实中有动人之处的人生故事，但不要太矫情。

## 人物画像
{{persona}}

## 人生分岔点
- 时间：{{fork_date}}
- 标题：{{fork_title}}
- 背景：{{fork_description}}
- 实际的选择：{{actual_choice}}
- 未走的那条路：{{alternative_choice}}

## 问卷中透露的更多信息
{{quiz_answers}}

## 写作要求
请以第一人称"我"的视角，写一篇关于"未走的那条路"的叙事故事，从分岔点推演到现在（{{current_year}}年）。

### 格式要求
- 用 `<!-- BLOCK n -->` 分隔符将故事分为 4-5 个叙事章节（n 从 1 开始）
- 每个 block 以 `## 章节标题` 开头
- 每个 block 约 500 字
- 使用 Markdown 格式（**粗体**、*斜体*等）

### 内容要求（核心：主次分明）
1. **先判断分岔点性质**：若分岔点较微小（如选哪家奶茶、穿哪件衣服、周末宅家还是出门），则文风云淡风轻、诙谐活泼，不要上升高度；若分岔点较重大（如考研/考公、结婚与否、换城市），则可立意深刻些。请自行判断后调整笔调。
2. **主体是"未走的路"**：故事绝大部分篇幅必须放在"如果选了另一条路之后"的人生展开，让用户看到另一种可能的日常生活、抉择、成长。用户来是为了看"另一种人生"，不是听自己过去的心路历程重述。
3. **分岔点只需简短带过**：用一两段交代当时做出不同选择的情境即可，不要花大量篇幅回溯纠结过程或真实的心理活动，然后迅速进入"选了另一条路之后"的推演。
4. 沿时间线展开，有高光时刻也有低谷和挣扎——因为每条路都有代价，在低谷挣扎中体现人性的坚韧和向上
5. 体现人物画像中的性格特征和价值观，让故事与"我"的性格吻合
6. 每个 block 包含具体的场景描写、对话、和内心独白，但细节可以含糊其辞，尽量让读者有看星座般“有些像我还挺准的”的感觉
7. 最后一个 block 可在结尾用几句话与"选了另一条路的自己"隔空对话，融入叙事自然带过，不要整段反思；**不要**在文中反复回溯"当时我其实选了考研/考公"等真实选择，用户已经知道——故事重点始终是"这条未走的路"上的事
8. 情感真实，不美化也不悲观，呈现一个真实的"另一种可能"
9. 不能自由生成过去的感情线，如生成未来的感情线应尽量开放，不能给其他角色取名
10. **不要给人随意取名**：文中人物一律用代称，如室友、那个她、好朋友、一个xx的男生、同事、学长等，不要编造具体人名，否则会出戏
11. **喜欢的菜**：问卷中提到的"喜欢吃的菜"全篇最多提一次，不要反复提及；若用户多次生成分叉点故事，同一道菜不要连续三个故事都出现（系统若提供了「近期故事中已提及的菜」则需避开）

### 硬性约束（必须遵守）
- **逻辑自洽**：全篇必须合逻辑，不能有任何前后矛盾。例如：前面说住在宿舍，后面就不能说和男友同居一年；居住状态、感情状态、工作学业等设定必须前后一致。
- **禁止瞎编情景**：不要随意编造用户未曾提及的具体情节（如"明天有期中考试"、"下周要见家长"等）。如需虚构剧情推进，应通过反问题提前向用户确认，否则会让读者出戏。只基于问卷和已有信息展开，细节可含糊带过。

### 输出示例结构
<!-- BLOCK 1 -->
## <标题1> 
（约500字叙事...）

<!-- BLOCK 2 -->
## <标题2> 
（约500字叙事...）

直接输出 Markdown 文本，不需要 JSON 格式。""",
    "story_questions": """你是一位故事策划师，正在为一位用户定制"未走的那条路"的人生故事。

## 人物画像
{{persona}}

## 人生分岔点
- 标题：{{fork_title}}
- 实际的选择：{{actual_choice}}
- 未走的那条路：{{alternative_choice}}

## 任务
为了让故事更贴合这个人的内心，AI结合用户前五部分的回答，找出与用户的人生分叉点相关但最模糊、最需要确认的3个点，生成三个问题。

每个问题提供 3-4 个差异化的选项；用户可选 1-2 个选项，并可补充自己的描述。每个问题的 hint 写：（选择1-2个选项，你还可以补充描述）

**原则**：若需要虚构具体剧情（如某次考试、某次见面等），应通过这些问题向用户确认，避免在故事中随意编造用户未提及的情景导致出戏。
然后根据用户的回答生成人生故事。

请以 JSON 格式输出：
{
  "questions": [
    {
      "id": "q1",
      "question": "问题内容",
      "hint": "（选择1-2个选项，你还可以补充描述）",
      "options": ["选项1", "选项2", "选项3"]
    }
  ]
}""",
    "story_generation": """你是一位才华横溢的叙事作家，擅长用细腻的笔触书写朴实中有动人之处的人生故事，但不要太矫情。

## 人物画像
{{persona}}

## 人生分岔点
- 时间：{{fork_date}}
- 标题：{{fork_title}}
- 背景：{{fork_description}}
- 实际的选择：{{actual_choice}}
- 未走的那条路：{{alternative_choice}}

## 问卷中透露的更多信息
{{quiz_answers}}

## 写作要求
请以第一人称"我"的视角，写一篇 1500-2500 字的 Markdown 故事，讲述如果当初选择了"未走的那条路"，我的人生会怎样展开。

### 格式要求
- 使用 Markdown 格式，包含标题（##）、段落、适当的强调（**粗体**、*斜体*）
- 故事标题用一级标题（#）
- 按时间线从分岔点推演到现在（{{current_year}}年）
- 分为多个章节（用 ## 二级标题），每个章节对应一个人生阶段

### 内容要求（核心：主次分明）
1. **先判断分岔点性质**：若分岔点较微小（如选哪家奶茶、穿哪件衣服），则文风云淡风轻、诙谐活泼，不要上升高度；若较重大（如考研/考公、结婚与否），则可立意深刻些。
2. **主体是"未走的路"**：故事绝大部分篇幅必须放在"如果选了另一条路之后"的人生展开——另一种工作/学业、日常、人际关系、成长。用户来是为了看"另一种人生"，不是听自己过去的心路历程重述。
3. **分岔点只需简短带过**：用一两段交代做出不同选择的情境即可，不要大段回溯纠结过程或真实心理，迅速进入"选了另一条路之后"的推演。
4. 沿时间线展开，有高光时刻也有低谷和挣扎——因为每条路都有代价
5. 体现人物画像中的性格特征和价值观，让故事与"我"的性格吻合
6. 包含具体的场景描写、对话、和内心独白，但细节可以含糊其辞，尽量让读者有看星座般"有些像我还挺准的"的感觉
7. 结尾可写一段与"选了另一条路的自己"隔空对话，自然收束；**不要**在文中反复回溯真实选择和纠结过程——故事重点始终是"这条未走的路"上发生的事
8. 情感真实，不美化也不悲观，呈现一个真实的"另一种可能"
9. **不要给人随意取名**：文中人物一律用代称，如室友、那个她、好朋友、一个xx的男生、同事、学长等，不要编造具体人名，否则会出戏
10. **喜欢的菜**：问卷中提到的"喜欢吃的菜"全篇最多提一次，不要反复提及；若用户多次生成分叉点故事，同一道菜不要连续三个故事都出现

### 硬性约束（必须遵守）
- **逻辑自洽**：全篇必须合逻辑，不能有任何前后矛盾。例如：前面说住在宿舍，后面就不能说和男友同居一年；居住状态、感情状态、工作学业等设定必须前后一致。
- **禁止瞎编情景**：不要随意编造用户未曾提及的具体情节（如"明天有期中考试"、"下周要见家长"等）。如需虚构剧情推进，应通过反问题提前向用户确认，否则会让读者出戏。只基于问卷和已有信息展开，细节可含糊带过。

直接输出 Markdown 故事文本，不需要 JSON 格式。""",

}


async def get_template(name: str, session: AsyncSession) -> str:
    """Get a prompt template by name. Falls back to hardcoded template if not in DB."""
    if name in _TEMPLATE_CACHE:
        return _TEMPLATE_CACHE[name]

    result = await session.execute(
        select(PromptTemplate).where(
            PromptTemplate.name == name,
            PromptTemplate.is_active == True,  # noqa: E712
        ).order_by(PromptTemplate.version.desc()).limit(1)
    )
    template = result.scalar_one_or_none()

    if template:
        _TEMPLATE_CACHE[name] = template.template
        return template.template

    fallback = FALLBACK_TEMPLATES.get(name)
    if fallback:
        return fallback

    raise ValueError(f"Prompt template '{name}' not found")


def render_template(template: str, variables: dict[str, str]) -> str:
    """Replace {{variable}} placeholders with actual values."""
    def replace_var(match: re.Match) -> str:
        key = match.group(1).strip()
        return variables.get(key, match.group(0))

    return re.sub(r"\{\{(\w+)\}\}", replace_var, template)


def clear_cache() -> None:
    _TEMPLATE_CACHE.clear()
